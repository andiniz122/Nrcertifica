# Foto do hero da home

Arquivo esperado: `hero.jpg` (também aceita `.jpeg`, `.png`, `.webp`).

Recomendação: horizontal, no mínimo 1200 px de largura. Fica no painel à direita
do título, em moldura arredondada e recorte `object-cover`.

Sem o arquivo, o hero usa a ilustração vetorial `components/HeroArte.tsx`.
